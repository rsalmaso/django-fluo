# -*- coding: utf-8 -*-

from django.conf.urls import url, patterns, include

urlpatterns = patterns('',
    # Example:
    # url(r'^/', '{{ app_name }}.views.index', name='{{ app_name }}-index'),
)

